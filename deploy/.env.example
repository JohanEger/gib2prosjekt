# Plasseres som ".env" ved siden av docker-compose.yml på VM-en.
# docker compose leser automatisk denne filen og bruker den til
# variabel-substitusjon i compose-filen (${DOCKER_USERNAME}).

# Docker Hub-brukernavnet til personen som eier image-repoene.
DOCKER_USERNAME=ditt-dockerhub-brukernavn
